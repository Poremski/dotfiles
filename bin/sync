#!/usr/bin/env bash

set -euo pipefail

script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
flake_dir="$(cd -- "${script_dir}/.." && pwd)"

cd "${flake_dir}"
git pull --ff-only
"${flake_dir}/bin/rebuild" "$@"
